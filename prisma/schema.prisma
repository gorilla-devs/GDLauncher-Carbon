datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "cargo prisma"
  output   = "../crates/carbon_minecraft/src/db.rs"
}

model AppConfiguration {
  id     Int   @id @default(autoincrement())
}