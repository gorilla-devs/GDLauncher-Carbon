---
/**
 * Modpacks Hub Page
 */
import PageShell from "../../../components/PageShell.astro"
import { DownloadLink } from "../../../components/DownloadLink"
import Breadcrumb from "../../../components/Breadcrumb.astro"
import TopAddonsList from "../../../components/TopAddonsList.astro"
import { getLocaleFromUrl, useTranslations,
  localizedPath,
} from "../../../lib/i18n"
import { addonTypePlural } from "../../../lib/api/types"

import type { GetStaticPaths } from "astro"
import { LOCALES, DEFAULT_LOCALE } from "../../../lib/i18n"

export const getStaticPaths: GetStaticPaths = () =>
  LOCALES
    .filter((loc) => loc !== DEFAULT_LOCALE)
    .map((loc) => ({ params: { locale: loc } }))


const locale = getLocaleFromUrl(Astro.url.pathname)
const t = useTranslations(locale)

Astro.locals.cache(300)
---

<PageShell
  title={t("modpacks.title")}
  description={t("modpacks.description")}
  keywords="minecraft modpacks, curseforge modpacks, modrinth modpacks, install modpacks, modpack launcher, gdlauncher"
>
    <main class="py-8">
      <Breadcrumb items={[{ label: addonTypePlural("modpacks", locale) }]} maxWidth="max-w-6xl" />

      <div class="max-w-6xl mx-auto px-4">
      <div class="bg-darkSlate-800 rounded-xl p-8 md:p-12 mb-12">
        <div class="text-center mb-12">
          <h1 class="text-4xl md:text-5xl font-bold text-lightSlate-50 mb-4">
            {t("modpacks.h1.line1")}{" "}
            <span class="text-primary-300">{t("modpacks.h1.line2")}</span>
          </h1>
          <p class="text-darkSlate-100 text-lg max-w-2xl mx-auto">
            {t("modpacks.intro")}
          </p>
        </div>

        <section class="prose prose-invert prose-lg max-w-3xl mx-auto mb-12">
          <h2 class="text-lightSlate-50">{t("modpacks.section.what.heading")}</h2>
          <p class="text-darkSlate-100">{t("modpacks.section.what.p1")}</p>
          <p class="text-darkSlate-100">{t("modpacks.section.what.p2")}</p>
          <ul class="text-darkSlate-100">
            <li>{t("modpacks.section.what.li.kitchen")}</li>
            <li>{t("modpacks.section.what.li.tech")}</li>
            <li>{t("modpacks.section.what.li.magic")}</li>
            <li>{t("modpacks.section.what.li.adventure")}</li>
            <li>{t("modpacks.section.what.li.lightweight")}</li>
          </ul>
        </section>

        <section class="prose prose-invert prose-lg max-w-3xl mx-auto mb-12">
          <h2 class="text-lightSlate-50">{t("modpacks.section.complexity.heading")}</h2>
          <p class="text-darkSlate-100">{t("modpacks.section.complexity.intro")}</p>
          <ul class="text-darkSlate-100">
            <li>{t("modpacks.section.complexity.li.lightweight")}</li>
            <li>{t("modpacks.section.complexity.li.medium")}</li>
            <li>{t("modpacks.section.complexity.li.heavy")}</li>
          </ul>
          <p class="text-darkSlate-100">{t("modpacks.section.complexity.outro")}</p>
        </section>

        <section class="prose prose-invert prose-lg max-w-3xl mx-auto">
          <h2 class="text-lightSlate-50">{t("modpacks.section.choose.heading")}</h2>
          <p class="text-darkSlate-100">{t("modpacks.section.choose.intro")}</p>
          <ul class="text-darkSlate-100">
            <li>{t("modpacks.section.choose.li.progression")}</li>
            <li>{t("modpacks.section.choose.li.theme")}</li>
            <li>{t("modpacks.section.choose.li.difficulty")}</li>
            <li>{t("modpacks.section.choose.li.multiplayer")}</li>
            <li>{t("modpacks.section.choose.li.updates")}</li>
          </ul>
          <p class="text-darkSlate-100">{t("modpacks.section.choose.outro")}</p>
        </section>
      </div>

      <TopAddonsList type="modpacks" />

      <section class="mb-12">
        <div class="bg-darkSlate-800 rounded-xl overflow-hidden">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
            <div class="p-8 lg:p-12">
              <h2 class="text-2xl md:text-3xl font-bold text-lightSlate-50 mb-4">{t("modpacks.browse.heading")}</h2>
              <p class="text-darkSlate-100 mb-6">{t("modpacks.browse.intro")}</p>
              <ul class="flex flex-col gap-3 mb-8">
                <li class="flex items-center gap-3 text-darkSlate-50">
                  <svg class="w-5 h-5 text-primary-400 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12l5 5L20 7"/></svg>
                  <span>{t("modpacks.browse.li.thousands")}</span>
                </li>
                <li class="flex items-center gap-3 text-darkSlate-50">
                  <svg class="w-5 h-5 text-primary-400 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12l5 5L20 7"/></svg>
                  <span>{t("modpacks.browse.li.oneclick")}</span>
                </li>
                <li class="flex items-center gap-3 text-darkSlate-50">
                  <svg class="w-5 h-5 text-primary-400 flex-shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12l5 5L20 7"/></svg>
                  <span>{t("modpacks.browse.li.updates")}</span>
                </li>
              </ul>
              <DownloadLink client:idle class="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-primary-500 hover:bg-primary-600 text-lightSlate-50 font-semibold transition-colors transition-transform duration-100 ease-spring active:scale-95" iconClass="w-5 h-5" label={t("addon.downloadCta")} iconLeft />
            </div>
            <div class="relative">
              <img
                src="https://cdn.gdl.gg/assets/website-browse-modpacks.jpg"
                alt="GDLauncher modpack browser interface"
                class="w-full h-auto"
                loading="lazy"
              />
            </div>
          </div>
        </div>
      </section>

      <section class="mt-8 bg-darkSlate-800 rounded-xl p-8 md:p-12">
        <h2 class="text-2xl font-bold text-lightSlate-50 mb-6 text-center">{t("modpacks.learnmore.heading")}</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 max-w-3xl mx-auto">
          <a href={localizedPath("/guides/install-modpack", locale)} class="p-4 rounded-lg bg-darkSlate-700/50 hover:bg-darkSlate-700 transition-colors transition-transform duration-100 ease-spring active:scale-98">
            <span class="text-lightSlate-50 font-medium">{t("modpacks.learnmore.installmodpacks")}</span>
          </a>
          <a href={localizedPath("/guides/allocate-more-ram", locale)} class="p-4 rounded-lg bg-darkSlate-700/50 hover:bg-darkSlate-700 transition-colors transition-transform duration-100 ease-spring active:scale-98">
            <span class="text-lightSlate-50 font-medium">{t("modpacks.learnmore.allocateram")}</span>
          </a>
          <a href={localizedPath("/blog/best-modpacks-2026", locale)} class="p-4 rounded-lg bg-darkSlate-700/50 hover:bg-darkSlate-700 transition-colors transition-transform duration-100 ease-spring active:scale-98">
            <span class="text-lightSlate-50 font-medium">{t("modpacks.learnmore.bestmodpacks")}</span>
          </a>
        </div>
      </section>
      </div>
    </main>
</PageShell>
