---
import type { GetStaticPaths } from "astro"
import AddonInstallLayout from "../../../components/AddonInstallLayout.astro"
import type { AddonInfo } from "../../../lib/api/types"
import { marked } from "marked"
import addonsData from "../../../data/addons.json"

import shadersInstructions from "../../../content/addon-instructions/shaders.md?raw"

export const getStaticPaths: GetStaticPaths = async () => {
  return addonsData.modrinth.shaders.map((addon) => ({
    params: { slug: addon.slug },
    props: { addon: { ...addon, platform: "modrinth" as const, type: "shaders" as const } },
  }))
}

interface Props {
  addon: AddonInfo
}

const { addon } = Astro.props
const instructionsHtml = await marked(shadersInstructions)
---

<AddonInstallLayout
  addonName={addon.name}
  addonSlug={addon.slug}
  addonType="shaders"
  platform="modrinth"
  imageUrl={addon.imageUrl}
  websiteUrl={addon.websiteUrl}
  description={addon.description}
  authors={addon.authors}
  author={addon.author}
  categories={addon.categories}
  dateModified={addon.dateModified}
>
  <Fragment set:html={instructionsHtml} />
</AddonInstallLayout>
