{
  "name": "@gd/carbon_minecraft",
  "version": "0.0.0",
  "scripts": {
    "prebuild": "cargo run --manifest-path ../prisma-cli/Cargo.toml --bin prisma-cli -- migrate reset --force --schema=./prisma/schema.prisma",
    "test": "pnpm prebuild && cargo test -- --nocapture",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings"
  }
}
